// // Имитация эффекта печатной машинки с задержками

// async function typewriter(text, delay = 50) {
//     for (let char of text) {
//         process.stdout.write(char);
//         await new Promise(resolve => setTimeout(resolve, delay));
//     }
//     console.log();
// }

// // Начало с небольшой задержкой
// setTimeout(async () => {
//     await typewriter("Загрузка началась...");
// }, 600);

// // Проценты идут быстрее (каждую секунду)
// setTimeout(async () => {
//     await typewriter("21%");
// }, 2000);  // Через 1.4 сек после первого

// setTimeout(async () => {
//     await typewriter("78%");
// }, 3000);  // Через 1 сек после предыдущего

// setTimeout(async () => {
//     await typewriter("99%");
// }, 4000);  // Через 1 сек после предыдущего

// // Финальное сообщение с бóльшей паузой для драматизма
// setTimeout(async () => {
//     // Добавляем небольшую паузу перед финалом
//     await new Promise(resolve => setTimeout(resolve, 800));
//     await typewriter("Загрузка завершена. Приятного пользования!");
//     await typewriter("=====================================");
// }, 5500);  // Через 1.5 сек после 99%



// // повторение функций
// // функция это блок кода, который выполняет определённое действие
// // чтобы использовать функцию, её нужно вызвать
// function info() {
//     console.log("привет");
// }
// info();
// console.log(info);


// function sum(a, b) {
//     return a + b;
// }
// console.log(sum(5, 10));


// Функция — это кусок кода с именем, который можно:
// вызвать
// использовать много раз
// передавать ему данные

function sayHello() {
    console.log("привет!");
}
sayHello(); // вызов функции

// Функция с параметрами
function sum(a, b) {
    return a + b;
}

const result = sum(5, 10);
console.log(result); // выводит 15
// a и b — параметры функции, которые принимают значения при вызове функции.
// return используется для возврата значения из функции обратно в место её вызова. Без return функция ничего не отдаёт.


setTimeout(() => { 
  console.log("Done");
}, 1000);
// setTimeout — это встроенная функция JavaScript, которая выполняет указанный код (первый аргумент) после заданной задержки в миллисекундах (второй аргумент).
// В данном случае, через 1000 миллисекунд (1 секунду) будет выведено сообщение "Done".


// Асинхронные функции и задержки
const processFile = async () => {
  console.log("Upload...");

  await wait(2000);
  console.log("Connect to DataBase");

  await wait(1000);
  console.log("Done");
};

processFile();
// это пример асинхронной функции, которая выполняет задачи с задержками между ними.
// async — разрешает использовать await
// await — ждёт, пока операция завершится, прежде чем продолжить выполнение кода дальше

async function orderPizza() {
  console.log("Заказал пиццу");

  await wait(3000);
  console.log("Пицца приехала");

  console.log("Ем 🍕");
}

orderPizza();


// Обычная функция
function sum(a, b) {
  return a + b;
}

// Стрелочная функция
const sumArrow = (a, b) => {
  return a + b;
};
// или даже короче:
const sumAB = (a, b) => a + b;

// пример
const numbers = [1, 2, 3];
const doubled = numbers.map(n => n * 2);
console.log(doubled); // [2, 4, 6]
// map — это метод массива, который создаёт новый массив, применяя указанную функцию к каждому элементу исходного массива.

// Promise = обещание, что результат будет позже
const promise1 = new Promise((resolve, reject) => {
  setTimeout(() => {
    resolve("Готово!");
  }, 2000);
});
promise1.then(result => {
  console.log(result);
});
// пример с использованием Promise и async/await
promise
  .then(result => console.log(result))
  .catch(error => console.log(error));

// Стрелка — коротко и без this
// Promise — результат в будущем
// async/await — удобный синтаксис
// Event Loop — порядок выполнения

function wait(ms) {
  return new Promise(resolve => setTimeout(resolve, ms));
}


const promise2 = new Promise((resolve, reject) => {
  setTimeout(() => {
    resolve("foo");
  }, 300);
});

promise1.then((value) => {
  console.log(value);
  // Expected output: "foo"
});

console.log(promise1);
// Expected output: [object Promise]

// Объект Promise используется для отложенных и асинхронных вычислений. 
// Он может быть в состоянии «выполнен» (fulfilled), «отклонен» (rejected) или «в процессе выполнения» (pending).
// ничего не понимаю
// пароль от hacknet: free277353.jp.net


const sleep = (ms) =>
  new Promise(resolve => setTimeout(resolve, ms));

const run = async () => {
  console.log("⏳ Начало");

  await sleep(1000);
  console.log("✅ Прошла 1 секунда");

  await sleep(500);
  console.log("🏁 Готово");
};

run();

// sleep это функция, которая возвращает промис с заданной задержкой
// run это асинхронная функция, которая вызывает sleep с заданной задержкой
// и выводит сообщения в консоль
// выводит сообщение "⏳ Начало"
// ждет 1 секунду
// выводит сообщение "✅ Прошла 1 секунда"
// ждет 0.5 секунды
// выводит сообщение "🏁 Готово"
// стрелочная функция это анонимная функция которая возвращает промис
// async/await это синтаксис для асинхронных операций
// промис это объект, который может быть в состоянии выполнен, отклонен или в процессе выполнения

// разобраться почему не работает терминал.

function wait(ms) {
  return new Promise(resolve => setTimeout(resolve, ms));
}

const promise3 = wait(1000);
console.log(promise);

// Expected output: Promise { <pending> }
// wait это функция, которая возвращает промис с заданной задержкой


// Игра "Угадай число"
// const secretNumber = Math.floor(Math.random() * 10) + 1;
// let attempts = 3;

// while (attempts > 0) {
//   const guess = Number(prompt(
//     `Угадай число от 1 до 10\nОсталось попыток: ${attempts}`
//   ));

//   if (guess === secretNumber) {
//     alert("🎉 Ты угадал!");
//     break;
//   } else if (guess > secretNumber) {
//     alert("Слишком большое число");
//   } else {
//     alert("Слишком маленькое число");
//   }

//   attempts--;
// }

// if (attempts === 0) {
//   alert(`😢 Попытки закончились. Было число: ${secretNumber}`);
// }

// wait это function













// get это метод, который возвращает промис












// обьяснение кода в lesson8

/* 
========================================
ПОДРОБНОЕ ОБЪЯСНЕНИЕ КОДА (Node.js + HTTP)
========================================

ОБЩАЯ ИДЕЯ:
------------
Этот файл запускает HTTP-сервер на Node.js.
Сервер:
- принимает запросы от браузера
- определяет, какой файл запрашивают
- читает этот файл с диска
- отправляет его браузеру
- умеет отдавать HTML, CSS, JS, видео, изображения

Это уже НЕ "учебный console.log",
а базовый настоящий сервер.


1. require("node:http")
---------------------
Модуль http нужен, чтобы:
- создать сервер
- принимать HTTP-запросы
- отправлять HTTP-ответы

Без него Node.js не может быть сервером.


2. require("node:fs")
---------------------
fs (file system) нужен для работы с файлами:
- читать HTML
- читать видео
- читать изображения

Мы используем createReadStream, чтобы:
- НЕ загружать файл целиком в память
- отправлять данные частями (stream)


3. require("node:path")
-----------------------
path нужен для корректной работы с путями.

Почему нельзя писать пути строкой?
- Windows и Linux используют разные слэши
- path.join() делает путь безопасным и универсальным


4. PORT = 3000
--------------
Порт — это "дверь", через которую браузер заходит на сервер.
Адрес сервера получается:
http://localhost:3000


5. MIME_TYPES
-------------
MIME-Type сообщает браузеру:
"Какой тип данных ты сейчас получаешь?"

Примеры:
- text/html → HTML страница
- video/mp4 → видео
- image/png → картинка

Если Content-Type неверный:
- браузер может не показать файл
- или показать как текст


6. http.createServer(...)
------------------------
createServer принимает функцию-обработчик.

Эта функция вызывается КАЖДЫЙ РАЗ,
когда браузер делает запрос.

Параметры:
- request  — информация о запросе (URL, метод и т.д.)
- response — объект, через который мы отвечаем браузеру


7. request.url
--------------
request.url — это путь, который запросил браузер.

Примеры:
- "/" → главная страница
- "/index.html" → файл
- "/video.mp4" → видео


8. Обработка главной страницы "/"
--------------------------------
Если request.url === "/":
- мы вручную подставляем index.html

Это нужно, потому что:
браузер не знает, что такое "главная страница",
он просто просит "/".


9. filePath
-----------
filePath — это полный путь к файлу на диске.

Мы собираем его через:
path.join(__dirname, "public", ...)

__dirname — это папка, где лежит server.js


10. path.extname(filePath)
--------------------------
extname возвращает расширение файла:
".html", ".css", ".mp4"

Мы убираем точку, чтобы:
- использовать ключи из MIME_TYPES


11. Content-Type
----------------
По расширению файла выбираем MIME-Type.
Если тип неизвестен — используем text/plain.


12. fs.createReadStream(filePath)
--------------------------------
Создаёт поток чтения файла.

Поток:
- читает файл частями
- экономит память
- идеально подходит для видео и больших файлов


13. response.setHeader(...)
--------------------------
Устанавливаем HTTP-заголовок Content-Type.
Браузер по нему понимает, как обрабатывать данные.


14. fileStream.pipe(response)
-----------------------------
pipe:
- соединяет поток файла и ответ сервера
- данные сразу идут браузеру
- response.end() вызывать не нужно


15. fileStream.on("error")
-------------------------
Если файл не найден или произошла ошибка:
- ловим ошибку
- отправляем статус 404
- закрываем соединение


16. server.listen(PORT)
----------------------
Запускает сервер.
После этого Node.js начинает "слушать" порт
и принимать запросы.


17. console.log(...)
-------------------
Просто выводит сообщение, что сервер запущен.


ИТОГ:
-----
Этот код:
- создаёт HTTP-сервер
- обрабатывает URL
- читает файлы потоками
- корректно отправляет HTML и видео
- использует базовые механизмы Node.js

Это фундамент для:
- Express
- API
- backend-разработки
========================================
*/







function addEvent(element, eventType, handler) {
  if (element) {
    element.addEventListener(eventType, handler);
  }
}

// Пример использования:
const button = document.getElementById('myButton');

// Добавляем обработчик клика
addEvent(button, 'click', function() {
  console.log('Кнопка нажата!');
});

// Добавляем обработчик наведения (mouseover)
addEvent(button, 'mouseover', () => {
  console.log('Курсор над кнопкой');
});










/*
=====================================================
GET / POST ЗАПРОСЫ, JSON И ВНЕШНИЕ API В JAVASCRIPT
=====================================================

ЭТО ОЧЕНЬ ВАЖНАЯ ТЕМА.
ПРАКТИЧЕСКИ ВЕСЬ BACKEND И FRONTEND КРУТИТСЯ ВОКРУГ ЭТОГО.


-----------------------------------------------------
1. ЧТО ТАКОЕ HTTP-ЗАПРОС ВООБЩЕ
-----------------------------------------------------

HTTP-запрос — это сообщение от клиента (браузера, приложения)
к серверу.

Пример:
Браузер говорит серверу:
"Дай мне страницу"
или
"Сохрани эти данные"

Запрос состоит из:
- метода (GET, POST, PUT, DELETE и т.д.)
- URL
- заголовков (headers)
- тела (body, есть не всегда)


-----------------------------------------------------
2. GET ЗАПРОС
-----------------------------------------------------

GET — это запрос ПОЛУЧЕНИЯ данных.

Главные свойства GET:
- ничего не изменяет на сервере
- НЕ имеет body (тела)
- данные передаются через URL
- используется для чтения

Примеры GET:
- загрузка сайта
- получение списка постов
- получение пользователя по id


Пример URL:
GET /users/5

Это значит:
"Сервер, дай пользователя с id = 5"


Пример в браузере:
fetch("https://api.example.com/users")
  .then(res => res.json())
  .then(data => console.log(data));

Здесь:
- fetch по умолчанию делает GET
- сервер возвращает данные
- мы их читаем


ВАЖНО:
GET НЕ ИСПОЛЬЗУЮТ для:
- паролей
- личных данных
- изменения информации


-----------------------------------------------------
3. POST ЗАПРОС
-----------------------------------------------------

POST — это запрос ОТПРАВКИ данных.

Главные свойства POST:
- имеет body (тело запроса)
- данные НЕ видны в URL
- используется для создания / отправки данных

Примеры POST:
- регистрация
- логин
- отправка формы
- создание записи


Пример:
POST /users

Тело запроса:
{
  "name": "Alex",
  "age": 20
}


Пример в JS:
fetch("https://api.example.com/users", {
  method: "POST",
  headers: {
    "Content-Type": "application/json"
  },
  body: JSON.stringify({
    name: "Alex",
    age: 20
  })
});

Здесь:
- method: POST
- headers говорят серверу, что это JSON
- body — данные, которые мы отправляем


-----------------------------------------------------
4. РАЗНИЦА GET И POST КОРОТКО
-----------------------------------------------------

GET:
- получить данные
- без body
- безопасный
- можно кешировать

POST:
- отправить данные
- есть body
- изменяет сервер
- не кешируется


-----------------------------------------------------
5. ЧТО ТАКОЕ JSON
-----------------------------------------------------

JSON (JavaScript Object Notation) —
это формат передачи данных.

Он очень похож на объект JS,
НО это СТРОКА.

Пример JSON:
{
  "name": "Alex",
  "age": 20,
  "isAdmin": false
}

Отличия от объекта:
- ключи ВСЕГДА в кавычках
- нельзя писать функции
- это просто текст


-----------------------------------------------------
6. JSON.stringify и JSON.parse
-----------------------------------------------------

JSON.stringify()
-----------------
Превращает объект JS в строку JSON.

Пример:
const obj = { name: "Alex" };
const json = JSON.stringify(obj);

Зачем?
- чтобы отправить данные на сервер


JSON.parse()
------------
Превращает строку JSON в объект JS.

Пример:
const data = JSON.parse(json);

Зачем?
- чтобы работать с данными в коде


-----------------------------------------------------
7. РАБОТА С JSON В fetch
-----------------------------------------------------

Когда сервер возвращает JSON,
мы делаем:

fetch(url)
  .then(res => res.json())
  .then(data => {
    // data — это уже JS-объект
  });

res.json():
- читает тело ответа
- парсит JSON
- возвращает Promise


-----------------------------------------------------
8. ЧТО ТАКОЕ API
-----------------------------------------------------

API — это интерфейс для общения программ.

Проще:
API — это сервер, который:
- принимает запросы
- возвращает данные
- не имеет интерфейса для человека


Примеры API:
- погода
- курсы валют
- фильмы
- карты
- авторизация


-----------------------------------------------------
9. ВНЕШНИЕ API
-----------------------------------------------------

Внешнее API — это API другого сервера.

Пример:
https://api.openweathermap.org
https://api.github.com
https://jsonplaceholder.typicode.com


Пример запроса:
fetch("https://api.github.com/users/octocat")
  .then(res => res.json())
  .then(data => console.log(data));


-----------------------------------------------------
10. STATUS CODES (КОДЫ ОТВЕТОВ)
-----------------------------------------------------

Сервер всегда отвечает кодом.

Основные:
- 200 — OK
- 201 — создано
- 400 — ошибка клиента
- 401 — не авторизован
- 403 — запрещено
- 404 — не найдено
- 500 — ошибка сервера


-----------------------------------------------------
11. ASYNC / AWAIT С FETCH
-----------------------------------------------------

То же самое, но читаемее:

async function loadData() {
  const response = await fetch(url);
  const data = await response.json();
  console.log(data);
}

Здесь:
- await ждёт Promise
- код читается как синхронный
- ошибок меньше


-----------------------------------------------------
12. ИТОГ
-----------------------------------------------------

GET:
- получить данные

POST:
- отправить данные

JSON:
- формат передачи данных

API:
- сервер для общения программ

fetch:
- инструмент для запросов

Promise / async:
- способ работать с асинхронностью


ЭТО БАЗА.
БЕЗ ЭТОГО НЕТ:
- backend
- frontend
- React
- Express
- API
=====================================================
*/





function dayStreak(days) {
  return days.reduce((acc, day) => acc + day, 0);
}


// fuck

// notes test
// test cimmit

//  dayStreak